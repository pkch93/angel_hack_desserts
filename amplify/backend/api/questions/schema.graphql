type Question @model{
  id: ID!
  contents: String!
  yes: Int!
  no: Int!
}

type Score @model{
  id: ID!
  score: Int!
  username: String!
  create_date: String!
}

type Insite @model{
  id: ID!
  category: Int!
  contents: String!
}
